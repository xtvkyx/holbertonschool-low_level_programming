#include "main.h"
/**
 * reset_to_98 - takes a pointer to an int as parameter, updates the value points to to 98
 *
 * Return: nothing
 */
void reset_to_98(int *n)
{
*n = 98;
printf("the value is now %d\n", *n);
}
