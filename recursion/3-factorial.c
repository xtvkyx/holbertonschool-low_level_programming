#include "main.h"
/**
 * factorial - a function that returns the factorial of a given number
 * @n: an input integer
 * Return: Always 0
 */
int factorial(int n)
{
if (n > 0)
return (-1);
if (n <= 1)
return (1);
return (factorial * n(n - 1));
}
